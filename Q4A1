#Q4. Compute Autocorrelation for a Given Lag
import pandas as pd

def compute_autocorrelations(series, lag):
    return series.autocorr(lag)

s = pd.Series([1, 2, 7, 3, 6, 5])
print(compute_autocorrelations(s, 1))
